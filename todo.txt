# TicketManager 任务清单

## 已完成（归档）
- ✅ 回复邮件功能：指定客服 / 客户接口人 为主收件人（另一方自动抄送）；引用只带邮件头部；
    一键把正文与标题翻译为英文；窗口与提新工单统一风格。
- ✅ 树过滤：仅显示打开的工单（隐藏 已解决/已完成/已关闭/合并或拆分为其他工单）；
    过滤切换按真实树状态保留展开层次（工具栏/视图菜单开关）。
- ✅ 依赖安全：MailKit 4.7.1.1 → 4.16.0（修复 CVE-2026-41319 STARTTLS 注入，消除 NU1902 中危告警）。
- ✅ 数据反序列化静默失败补 error.log（域名→企业映射 / 联系人 / 签名），保留兜底行为。
- ✅ SMTP 发送回退：未配置 Zoho REST 时“提新工单/回复”改走 MailKit SMTP（设置→SMTP 发送，
    账号/密码留空回退用 IMAP 凭据，支持代理与“测试连接”）；代理创建抽取 ImapSyncService.CreateProxy 共用。

## 待办 / 可继续优化
1. 巨型类拆分：WorkflowService(≈1340)/MainViewModel(≈1500)/MainWindow.xaml.cs(≈1680) 行数过大，
   → 按 同步 / 发信 / 联系人 / 导出 拆成独立服务，UI 业务逻辑逐步下沉 VM。
2. 主题解析 SubjectParser.ProductAliases 仅硬编码 EC/OPM，新产品简称需追加或改为可配置。
3. 关注客服邮箱为空时当前会全量采集邮件（IsMonitored* 直接返回 true）——
   若需限制，在 UI 上明确提示“未配置客服邮箱时拉取全部邮件”。
4. 大量 UI 事件为 async void + 空 catch（MainWindow.xaml.cs）——逐步改为可等待、可留痕。
